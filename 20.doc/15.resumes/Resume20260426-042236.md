# Resume 2026-04-26 04:22:36 (Asia/Taipei)

## 任務摘要

完成 `corn-dbio-kit` Rust 工具初始化與實作，支援依條件與分頁匯出資料表資料為 SQL 檔，並建立對應工作腳本、Makefile、變更紀錄。

## 主要產出

- 建立專案與依賴：`Cargo.toml`
  - `anyhow`, `dotenvy`, `odbc-api`, `regex`
- 實作主程式：`src/main.rs`
  - CLI 參數解析與 help
  - 支援資料庫型別：`mssql`, `oracle`, `mysql`, `postgresql`
  - `.env` 載入規則：優先 `../.env`，否則 `./.env`
  - 依 `tables` (regex) 篩選 table
  - 支援 `condition`, `recsize`, `page-size`(預設 10000), `page-no`
  - SQL 匯出檔名格式：`{table}_{page_index(6位補0)}.sql`
- 建立工作腳本
  - `util_compile.sh`
  - `util_corn-dbio-kit.sh`
  - `util_corn-dbio-kit-loop-exec.sh`
  - `util_all-in-one-compile.sh`
- 建立 `Makefile`
- 建立 `ChangeLog.md`
- 更新離線套件下載腳本
  - `datahub-task/jobs/package/rust/modules/download_all_package-cron.sh`
  - 由 `tools/dbc` 改為 `tools/corn-dbio-kit`

## 驗證結果

- 執行 `cargo check --manifest-path datahub-task/tools/corn-dbio-kit/Cargo.toml`
- 結果：成功 (Exit code 0)

## 注意事項

- ODBC driver 名稱可透過環境變數 `DB2SQL_ODBC_DRIVER` 覆寫，未設定時使用各 DB 預設 driver 字串。
- 目前輸出 SQL 為 `INSERT INTO {table} VALUES (...)`（未展開欄位名稱）。
